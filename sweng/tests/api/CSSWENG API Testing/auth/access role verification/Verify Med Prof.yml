info:
  name: Verify Med Prof
  type: http
  seq: 2

http:
  method: GET
  url: "{{SUPABASE_URL}}/rest/v1/profiles?id=eq.{{med_prof_id}}&select=role"
  headers:
    - name: apikey
      value: "{{SUPABASE_ANON_KEY}}"
    - name: Authorization
      value: Bearer {{med_prof_token}}
  params:
    - name: id
      value: eq.{{med_prof_id}}
      type: query
    - name: select
      value: role
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Profile request was successful", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("User has the correct enum role", function() {
          const data = res.getBody();
          
          // Verify the array exists and isn't empty
          expect(data).to.be.an('array').that.is.not.empty;
          
          // Check the role of the first object in the array
          expect(data[0].role).to.equal("med_prof"); 
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
