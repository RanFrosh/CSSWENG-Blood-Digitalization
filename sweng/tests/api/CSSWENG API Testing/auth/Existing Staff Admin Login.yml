info:
  name: Existing Staff Admin Login
  type: http
  seq: 6

http:
  method: POST
  url: "{{SUPABASE_URL}}/auth/v1/token?grant_type=password"
  headers:
    - name: apikey
      value: "{{SUPABASE_ANON_KEY}}"
    - name: Content-Type
      value: application/json
  params:
    - name: grant_type
      value: password
      type: query
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "email": {{staff_admin_email}},
        "password": {{staff_admin_pw}}
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Staff Admin successfully logs in and receives tokens", function() {
          expect(res.getStatus()).to.equal(200);
          
          const body = res.getBody();

          expect(body).to.have.property('access_token');
          expect(body).to.have.property('refresh_token');
          
          expect(body).to.have.property('user');
          
          bru.setEnvVar("staff_admin_token", body.access_token);
          bru.setEnvVar("staff_admin_id", body.user.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
